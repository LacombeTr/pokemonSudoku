import { OtherConditionsChipPipe } from './other-conditions-chip.pipe';

describe('OtherConditionsChipPipe', () => {
  it('create an instance', () => {
    const pipe = new OtherConditionsChipPipe();
    expect(pipe).toBeTruthy();
  });
});
